[plugin]
name = "kv-store"
version = "0.1.0"
description = "KV store management — API, admin UI, and CLI"
entry = "init.lua"

[permissions]
routes = true
commands = true
admin = true
database = "write"
